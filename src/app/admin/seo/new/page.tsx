import React from "react";
import SeoForm from "@/components/admin/SeoForm";

export const metadata = {
  title: "Add New SEO Route | Admin",
};

export default function NewSeoPage() {
  return <SeoForm />;
}
